// Crash Bandicoot: Warped (`SCUS-94244`) resident recompiler seeds.
//
// psxport independently roots discovery at this executable's entry, scans its binary pointers and
// tables, and follows direct calls. These lists are deliberately empty. Add an address only after a
// real `SCUS_942.44` run reports an indirect-only `[recomp-MISS]`, with evidence that the address
// belongs to this executable. Never import addresses from another Crash title or from DRAGON/SPYRO.EXE.
{
  "main": [],
  "main_reentry": [],
  "overlay_bases": {},
  "overlay_base_patterns": [],
  "overlay_seeds": {}
}
