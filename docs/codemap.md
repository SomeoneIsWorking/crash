# Codemap

| Subsystem | Status | Where | Gap / next |
|---|---|---|---|
| Framework consumer | 🟡 current-frontier product | `CMakeLists.txt`, `external/psxport/`, `psxport.pin` | Clean Clang product build, CTest 6/6, and all three 13/13 serial boundary gates use recorded psxport `9c2e3f1c`; Crash 2/3 remain boundary integrations |
| Player launcher | 🟡 Crash 1 frontier | `run.sh`, `bootstrap.py`, `tools/run.py`, `pyproject.toml`, `uv.lock`, `tests/test_run.py` | Zero arguments select the real `crash1_port` product through one frozen-uv interpreter; the isolated player tree configures with `BUILD_TESTING=OFF`, provisions/emits/builds only required product inputs, and never runs CTest or diagnostics. Seven hermetic cases prove the positive route and refusals. Later boot and Crash 2/3 products remain missing |
| Boundary-only runtime policy | ✅ verified policy | `game/core/` (`boundary_runtime.h`, `boundary_runtime.cpp`), title runtime classes | Crash 1/2/3 direct-inheritance tests pass against pinned `9c2e3f1c`; each serial explicitly reports no guest-picture ownership because no frame producer exists, and no inferred Crash engine behavior satisfies SHARED-01 |
| Crash 1 runtime/product seam | 🟡 current frontier | `titles/crash1/core/crash1_runtime.*`, `titles/crash1/core/crash1_port.*`, `titles/crash1/main.cpp`, `tests/crash1_runtime*.cpp` | `crash1_port` loads the retail program, dispatches generated crt0, reaches the first measured syscall, performs the shipping HLE transition, and stops with the Cause/EPC-resume blocker. No frame, input loop, audio, or later boot exists |
| Authoritative verifier | ✅ enforced | `tools/verify.py`, `scratch/build-clang/` | The normal entry point configures and builds the documented Clang tree before full CTest and pin checking, preventing stale generated test graphs |
| C++ verification | ✅ enforced | `.clang-format`, `.clang-tidy`, `CMakeLists.txt`, `tools/verify.py`, `external/psxport/tools/check_cpp_style.py` | `tools/verify.py` explicitly selects Clang for authoritative evidence; top-level CMake has no compiler identity allowlist/denylist. Normal CTest runs format, 1,200-line ownership, and compile-database clang-tidy checks; generated code remains excluded |
| Executable identity gate | ✅ verified | `tools/verify_executable.py` | All three title manifests are compared to real images through psxport's PS-X EXE loader; each 4/4 positive/negative selftest passes |
| Serial-aware disc provisioning | ✅ verified | `tools/provision_title.py`, `tests/test_provision_titles.py` | One shared title-selected CLI/env/`.env`/drop-in implementation; 10 cases include three-way key isolation and prove Crash 3 follows `SYSTEM.CNF`, not the bundled Spyro demo executable |
| Project registries | 🟡 partial | `docs/info/`, `docs/issues/`, `docs/re-frontier.md` | Data exists and the shared skill engines work; local command entrypoints remain absent (issue #1) |
| Crash 1 executable identity | ✅ verified | `titles/crash1/executable.json`, `tools/verify_executable.py` | North American `SCUS-94900`: the real USA disc's `SYSTEM.CNF` selects `SCUS_949.00`, whose 290,816 bytes match all tracked facts |
| Resident program execution | 🟡 syscall boundaries | `game/core/resident_program.*`, `game/core/enter_critical_frontier.*`, `tools/resident_recomp.py`, `tests/recomp_boundary_support.h`, CMake `crash{1,2,3}_recomp_boundary_check` | The Crash 1 product and all serial-scoped harnesses use the same generated-program setup and measured `EnterCriticalSection` implementation. Crash 1's 13/13 suite tracks game main and syscall ordinals by manifest; the oracle cannot yet resume any syscall |
| Crash 2 integration | 🟡 syscall boundary | `titles/crash2/executable.json`, `titles/crash2/core/crash2_runtime.*`, `tools/scus_94154_recomp.py`, CMake `crash2_recomp_boundary_check` | `SCUS-94154` identity and typed runtime match real bytes; eight generated/oracle calls agree 34/34 through game main `0x80049BD4` and syscall wrapper `0x80049D1C`. Post-syscall equality and first frame remain blocked |
| Crash 3 integration | 🟡 syscall boundary | `titles/crash3/executable.json`, `titles/crash3/core/crash3_runtime.*`, `tools/scus_94244_recomp.py`, CMake `crash3_recomp_boundary_check` | `SCUS-94244` identity and typed runtime match real bytes; eight generated/oracle calls agree 34/34 through game main `0x80048AA0` and syscall wrapper `0x80048C38`. Post-syscall equality and first frame remain blocked |
| Shared engine | ⬜ missing | `game/` | Begins only after cross-title RE proves ownership |
| Native graphics producers | ⬜ missing | — | No producer exists |
| Widescreen | ⬜ missing | — | Blocked on native camera and producers |
| Interpolation | ⬜ missing | — | Blocked on PC ownership of transform producers |
| Differential harness | 🟡 title boundaries | CMake oracle targets plus all three `crash*_recomp_boundary_check` targets; `tests/recomp_boundary_support.h`; shipping `game/core/` execution owners | Crash 1, 2, and 3 independently reach their serial-scoped syscall boundaries. Crash 1's product shares the port-side boundary implementation rather than duplicating it. No result licenses post-syscall equality |
